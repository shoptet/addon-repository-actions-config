{
  "name": "addon-repository-actions-config",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "check:format": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
